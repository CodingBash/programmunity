package com.programmunity.webapplication.controllers;

import org.junit.Ignore;
import org.junit.Test;

@Ignore
public class HomeControllerTest extends BaseController
{

	/*
	 * @Test public void testHomePage() throws Exception{ HomeController
	 * controller = new HomeController(); MockMvc mockMvc =
	 * standaloneSetup(controller).build();
	 * mockMvc.perform(get("/")).andExpect(view().name("home")); }
	 */
}
